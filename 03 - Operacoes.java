
public class Operacoes
{
    double somaNumeros(double numero1, double numero2)
    {
        double soma;
        //Variável que receberá a soma dos dois perâmetros: "numero1" e "numero2"
        soma = numero1 + numero2;
        //Calcula a soma
        return soma;
        //Retorna o valor da soma
    }
    //Método que calculará a soma dos parãmetros caso a variável de escolha "escolha" 
    //da classe com o main o chame
    
    double subtrNumeros(double numero1, double numero2)
    {
        double diferenca;
        //Variável que receberá a subtração dos dois perâmetros: "numero1" e "numero2"
        diferenca = numero1 - numero2;
        //Calcula a subtração
        return diferenca;
        //Retorna o valor da subtração
    }
    //Método que calculará a subtração dos parãmetros caso a variável de escolha "escolha" 
    //da classe com o main o chame
    
    double multNumeros(double numero1, double numero2)
    {
        double produto;
        //Variável que receberá a multiplicação dos dois perâmetros: "numero1" e "numero2"
        produto = numero1 * numero2;
        //Calcula a multiplicação
        return produto;
        //Retorna o valor da multiplicação
    }
    //Método que calculará a multiplicação dos parãmetros caso a variável de escolha "escolha" 
    //da classe com o main o chame
    
    double divNumeros(double numero1, double numero2)
    {
        double divisao;
        //Variável que receberá a divisão dos dois perâmetros: "numero1" e "numero2"
        divisao = numero1 / numero2;
        //Calcula a divisão
        return divisao;
        //Retorna o valor da divisão
    }
    //Método que calculará a multiplicação dos parãmetros caso a variável de escolha "escolha" 
    //da classe com o main o chame
    
    
    
}
